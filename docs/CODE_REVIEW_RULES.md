# Code Review Rules

Required for PRs:
- Unit + Integration + BDD tests present
- Coverage >= 80%
- Linting passes
- Follow naming conventions: camelCase for Java vars, PascalCase for classes, kebab-case folders
- Commit messages: conventional commits
- Security: dependency scanning and secret detection in CI
